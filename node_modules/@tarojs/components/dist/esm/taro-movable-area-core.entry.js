import { r as registerInstance, h, H as Host } from './core-f216d924.js';

const MovableArea = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
    }
    componentDidLoad() {
        console.error('H5 暂不支持 MovableArea 组件！');
    }
    render() {
        return (h(Host, null));
    }
};

export { MovableArea as taro_movable_area_core };
